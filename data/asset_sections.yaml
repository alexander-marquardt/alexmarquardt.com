# Optional prose for a directory under static/ecommerce-demo-assets/images/.
#
# Keyed by directory name. A directory with no entry here still gets its page
# and its listing -- this file adds provenance, it does not declare sections.
# Nothing here lists a file, so adding images never means editing this.

groceries:
  blurb: >-
    Product photographs collected for demo use.

electronics:
  blurb: >-
    Product photographs collected for demo use.

musgrave:
  blurb: >-
    Product photographs collected for demo use.

industrial:
  blurb: >-
    Dimensioned technical drawings of a synthetic industrial-supply catalog,
    one directory per product type. **Nothing here is a photograph and nothing
    here was generated by a model.** Each drawing is plotted from the attribute
    values of one generated product record by the
    [synthetic-industrial-products](https://github.com/alexander-marquardt/synthetic-industrial-products)
    generator, so it cannot show a dimension the record does not carry — and
    the products it depicts do not exist.


    A drawing is shared by a whole family rather than belonging to one
    product, which is what a real supplier's catalog does — several sizes of
    the same end mill are listed against the same picture. So a file is named
    for what it draws, not for any one SKU, and many catalog records resolve
    to the same URL. The directory names the product type **drawn**.
